# 数据分析赛道 · 项目实现路径

> 赛项：传智杯 AI WEB网页开发挑战赛（赛道 10037）
> 方向：官方推荐方向 8「AI数据分析与可视化」
> 省赛截止：2026-11-18

---

## 一、产品定位（一句话）

**面向高校教学管理的数据分析 Agent，核心能力是"回答前先问清楚口径"。**

不是"套了个对话框的 BI 工具"，而是一个**知道自己可能理解错、并且主动澄清**的分析助手。
每次给出数字都附带：这个指标怎么定义的、来自哪个数据源、统计周期是什么、不同口径差多少。

### 为什么这个定位能赢

真实教学管理里最痛的不是"不会查数据"，而是**同一个指标各部门口径不一致**：
校领导问"我们退学率多少"，教务处按学籍异动算一个数，学院按课程退课算另一个数，学工按失联人数算第三个。三个数都对，但会上吵架。

市面上的自然语言 BI 产品（以及绝大多数参赛作品）都不解决这个问题——它们假设口径是唯一的，直接给一个数。

**而这正好是评分表上权重最高的几项**：Agent 编排（8%）、RAG 检索（6%）、安全可控中的可解释性（4%）、选题创新（10%）。

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────┐
│  前端 Next.js 14 App Router + Tailwind + shadcn/ui      │
│  ├ 对话区（流式输出 + 思考过程可视化）                    │
│  ├ 图表区（生成式 UI，AI 决定图表类型）                   │
│  ├ 溯源面板（SQL / 口径定义 / 数据来源 / 统计周期）        │
│  └ 口径对比视图（多口径并列 + 差异高亮）                  │
└─────────────────────────────────────────────────────────┘
                          ↕ SSE 流式
┌─────────────────────────────────────────────────────────┐
│  Agent 编排层（Vercel AI SDK + Tool Calling）            │
│  规划 → 检索口径 → 澄清追问 → 生成查询 → 安全校验          │
│      → 执行 → 结果校验 → (重试) → 选图 → 总结            │
└─────────────────────────────────────────────────────────┘
        ↕                    ↕                    ↕
┌──────────────┐  ┌──────────────────┐  ┌─────────────────┐
│ 指标口径库    │  │ 数据查询层        │  │ MCP Server      │
│ (向量+结构化) │  │ (只读+行级权限)   │  │ 封装数据库/BI   │
│ PostgreSQL   │  │ DuckDB/Postgres  │  │                 │
│ + pgvector   │  │                  │  │                 │
└──────────────┘  └──────────────────┘  └─────────────────┘
                          ↑
        ┌─────────────────┴─────────────────┐
        │ 数据源层（三源，口径天然不同）      │
        │ ① OULAD 微观学习行为数据           │
        │ ② 教育部宏观统计数据（1997-2024）  │
        │ ③ 全国高校名单（维度表）           │
        └───────────────────────────────────┘
```

---

## 三、功能清单

按 **P0（必做）/ P1（应做）/ P2（加分）** 分级。P0 不做完不碰 P1。

### P0-1 自然语言问答分析（核心入口）

| 项 | 说明 |
| --- | --- |
| 输入 | 中文自然语言，如"去年哪些专业挂科最严重" |
| 处理 | 意图识别 → 实体抽取（时间/指标/维度） → 生成 SQL → 执行 |
| 输出 | 结论文字 + 图表 + 可展开的 SQL |
| 交互 | 流式输出，边生成边显示 |

**验收标准**：输入 20 个预设问题，至少 15 个能给出正确答案并出图。

### P0-2 口径澄清与追问（差异化核心，最重要）

| 项 | 说明 |
| --- | --- |
| 触发 | 检测到问题中指标存在多口径，或缺少必要限定 |
| 行为 | **不直接回答**，先反问澄清 |
| 示例 | 用户问"退学率多少" → AI 反问"您指学籍异动退学、课程退课，还是学习参与度低于阈值？三者口径不同，数值差异较大" |
| 交互 | 提供可点击的选项卡片，而非让用户打字 |
| 兜底 | 用户选"都要"时，并列展示多口径对比，高亮差异 |

**为什么要专门做这个**：这是整个项目最能打动评委的地方。它同时证明了 Agent 有自主判断力、RAG 检索到了口径库、系统对"不确定性"有处理机制。

**验收标准**：演示时，同一个问题在两个不同口径下给出两个不同数字，且系统能解释为什么不同。

### P0-3 指标口径库（治理底座）

| 项 | 说明 |
| --- | --- |
| 内容 | 每个指标包含：名称、别名、口径定义、计算公式、数据来源、统计周期、责任部门、口径差异说明 |
| 规模 | 首期整理 25-30 个核心指标即可 |
| 检索 | 向量检索（语义匹配别名与口语说法）+ 结构化过滤 |
| 示例指标 | 退学率、挂科率、课程通过率、学习参与度、活跃学习天数、师生比、生均经费、招生完成率 |

**落地**：手工整理成 YAML/JSON 后入库。这部分是**脏活但没有捷径**——口径库的质量直接决定项目上限。

### P0-4 多步 Agent 编排

流水线节点（每个节点都是可观测的）：

1. **意图解析**：用户到底问什么？涉及哪些指标和维度？
2. **口径检索**：查口径库，判断是否存在歧义
3. **澄清**（条件触发）：有歧义则反问，等用户确认
4. **查询生成**：生成只读 SQL
5. **安全校验**：语法白名单、表权限、强制 LIMIT、注入检测
6. **执行**
7. **结果校验**：空结果、异常值、行数异常 → 自动修正重试（最多 2 次）
8. **图表决策**：根据数据形态选图表类型（趋势→折线，占比→饼图，对比→柱状，分布→直方图）
9. **结论生成**：基于真实数据写结论，禁止编造数字

**要求**：每一步的状态在前端可见（"正在检索指标口径…"），这就是"AI交互体验"那 3 分。

### P0-5 数据安全与权限

| 功能 | 说明 |
| --- | --- |
| 只读连接 | 数据库账号仅 SELECT 权限 |
| 语句白名单 | 只允许 SELECT，拦截 DDL/DML |
| 强制 LIMIT | 自动追加，防止全表扫描 |
| 注入防护 | 参数化 + 关键词检测 |
| 行级权限 | 不同角色看到不同范围数据（可简化为演示两个角色） |
| SQL 可见 | 生成的 SQL 对用户完全可见 |

### P0-6 结果可追溯

每次回答附带的溯源信息：

- 生成的 SQL（可复制）
- 命中的口径条目（含定义全文）
- 数据来源（OULAD / 教育部 / 高校名单）及统计周期
- 数据更新时间

**交互**：溯源信息折叠在回答下方，点击可展开。这是"AI安全可控"4 分和可解释性的直接体现。

### P0-7 流式输出与思考态可视化

- 文字逐字流出（SSE）
- 思考过程分步展示（不是转圈，而是"正在检索口径库→命中 3 条→正在生成查询"）
- 错误边界：查询失败时给出人话解释 + 建议，而不是抛异常栈

---

### P1-1 多模态数据接入（补多模态 5 分）

| 入口 | 说明 |
| --- | --- |
| 报表截图识别 | 上传纸质报表/Excel 截图 → 多模态模型识别 → 结构化入库 → 立即参与分析 |
| 语音提问 | Web Speech API，说"看下上个月华东区的情况" |

**为什么放在这个位置**：它解决了真实痛点——很多学校的历史数据只有纸质报表和截图，进不了数据库。这个功能让系统能"消化"这些数据。

**演示价值极高**：现场拍一张报表照片，几秒后就能对它提问分析。

### P1-2 主动洞察发现

- 后台扫描数据，自动发现异常（某课程挂科率突增、某群体参与度下降）
- 在首页以"洞察卡片"形式呈现
- 用户可点击深入追问

**价值**：从"被动问答"变成"主动分析"，体现 Agent 的自主性。

### P1-3 分析看板与路径回放

- 收藏的分析保存为看板卡片
- 分析路径可回放（每一步可点击回看）

### P2-1 归因分析

问题"为什么这门课挂科率高" → Agent 自动做多维度下钻（按班级、按前置课程、按学习行为），找出相关性最强的因素。

**注意**：必须明确标注"相关不等于因果"，这本身就是"AI安全可控"的体现。

### P2-2 报告导出

一键导出分析结论为 PDF/Markdown 报告。

---

## 四、技术选型

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 框架 | **Next.js 14+ App Router** | 官方推荐首选；API Routes 免后端；流式渲染原生支持 |
| AI SDK | **Vercel AI SDK** | 官方推荐首选；流式对话 + Tool Calling + 生成式 UI |
| 大模型 | DeepSeek / 智谱（成本低）或 Claude | 中文 SQL 生成效果需实测对比 |
| 多模态 | 通义千问 VL / GPT-4o | 报表截图识别 |
| 数据库 | **PostgreSQL + pgvector** | 一个库同时搞定关系查询和向量检索 |
| 分析引擎 | **DuckDB**（可选） | 对 440MB 的 CSV 直接查询，免导入，开发期极快 |
| 状态管理 | Zustand | 轻量 |
| 数据请求 | TanStack Query | 缓存与重试 |
| 图表 | **ECharts** | 中文生态好，类型全 |
| 样式 | Tailwind + shadcn/ui | 官方推荐；组件质量高 |
| 动画 | Framer Motion | AI 思考态过渡 |
| MCP | 自建 MCP Server 封装数据库 | 拿 MCP 4 分 |
| 部署 | Vercel | 免费、公网可访问（在线演示加分项） |
| 包管理 | pnpm | 官方推荐 |

---

## 五、数据层设计

### 三个数据源的天然口径冲突（这是项目的灵魂）

| 数据源 | 粒度 | 口径 | 能回答 |
| --- | --- | --- | --- |
| **OULAD** | 单个学生/单次点击 | 学习平台行为口径 | 学生流失、学习行为、成绩预测 |
| **教育部统计** | 全国/省级年度汇总 | 学籍统计口径 | 招生规模、在校生、师生比、经费 |
| **全国高校名单** | 学校维度 | 行政备案口径 | 院校分布、办学层次结构 |

**核心设计**：同一个问题"学生流失情况如何"，OULAD 给的是课程退课率，教育部给的是学籍异动率，**两个数字差异巨大且都正确**。
系统必须能解释这个差异——这就是口径治理功能的真实数据支撑。

### OULAD 表结构（八张表）

| 表 | 内容 | 分析用途 |
| --- | --- | --- |
| `studentInfo` | 学生属性 + **final_result**（Pass/Fail/Withdrawn/Distinction） | 退学预警的标签来源 |
| `studentVle` | **逐次点击流**（谁、哪天、点了哪个资源、次数） | 学习行为画像 |
| `studentAssessment` | 每次作业成绩与提交时间 | 学业表现趋势 |
| `studentRegistration` | 选课与退课时间 | **流失节点分析** |
| `vle` | 学习资源类型与所属周次 | 资源有效性评估 |
| `assessments` | 作业元数据（类型、权重、截止日） | 作业设计分析 |
| `courses` | 课程与开课期 | 维度表 |
| — | 关联键：`id_student` / `code_module` / `code_presentation` | — |

> **待办**：下载后需逐字段核验枚举值（`final_result`、`highest_education`、`age_band` 等），并记录在文档中。

### 数据入库策略

1. **OULAD**：CSV 直接入 PostgreSQL（`studentVle` 432MB 需分批导入）或用 DuckDB 直查
2. **教育部数据**：写抓取脚本 → 解析 HTML 表格 → 结构化入库（脚本本身是文档加分素材）
3. **高校名单**：抓取 → 作为维度表

### 指标口径库示例（YAML 结构）

```yaml
- code: dropout_rate_academic
  name: 退学率（学籍口径）
  aliases: [退学率, 辍学率, 退学比例]
  definition: 统计周期内学籍异动为"退学"的学生数 / 期初在校生数
  formula: count(status='退学') / count(期初在校生)
  source: 教育部统计数据
  period: 学年
  owner: 教务处
  caveat: 与"课程退课率"口径不同，后者按课程维度统计，重复计算同一学生

- code: dropout_rate_course
  name: 退课率（课程口径）
  aliases: [退课率, 课程流失率]
  definition: 在学习平台中退选课程的学生人次 / 选课总人次
  formula: count(withdrawn) / count(enrolled)
  source: OULAD
  period: 开课期
  owner: 教学平台
  caveat: 同一学生多门课会被重复统计，数值通常远高于学籍退学率
```

---

## 六、开发计划（倒推 2026-11-18，共约 59 天）

| 阶段 | 时间 | 交付物 | 卡点 |
| --- | --- | --- | --- |
| **W1-W2** | 9/20 - 10/3 | 建公开仓库（**每天提交**）、数据全部落地、跑通"提问→SQL→图"最小闭环 | 数据导入耗时 |
| **W3-W4** | 10/4 - 10/17 | 口径库（25-30 指标）、澄清追问、多步 Agent 编排 | **口径库是脏活，留足时间** |
| **W5** | 10/18 - 10/24 | SQL 安全校验、行级权限、流式输出、思考态可视化 | — |
| **W6** | 10/25 - 10/31 | 多模态入口（截图识别 + 语音）、MCP Server 封装 | OCR 准确率调优 |
| **W7** | 11/1 - 11/7 | 生成式图表、溯源面板、主动洞察、部署上线 | 部署环境问题 |
| **W8** | 11/8 - 11/14 | 录 5-8 分钟视频、写 ≤30 页 PDF 文档 | — |
| **W9** | 11/15 - 11/18 | 缓冲：修 bug、准备评审测试账号、提交 | 留 3-5 天 |

### 每周必须做的事
- 每天至少一次 commit（评分明确要求真实 Git 历史）
- 每两周写一次技术文档（别堆到最后）

---

## 七、演示脚本设计（决定成败的 5-8 分钟）

按这个顺序演示，层层递进：

**第 1 幕：常规能力（30 秒）**
问"去年哪些专业挂科最严重" → 秒出图表。建立基本预期。

**第 2 幕：杀手锏——口径澄清（90 秒）**
问"我们学校退学率多少" → **AI 不给数字，先反问**"您指学籍退学、课程退课，还是参与度下降？"
选择后给出数字 → 再展示"多口径对比视图"，同一问题的三个数字并列，差异高亮。
讲解："这三个数都对，口径不同。以前开会吵架就是因为这个。"

**第 3 幕：多步 Agent（90 秒）**
问"哪些学生有退学风险，为什么" → 展示思考过程逐步展开（检索口径 → 生成查询 → 校验 → 执行 → 发现异常 → 重试）
→ 输出风险清单 + 归因分析。

**第 4 幕：多模态（60 秒）**
现场拍一张纸质报表照片上传 → 识别 → 入库 → 立即提问分析。
讲解："很多学校的历史数据只有纸质报表，这是让它们进入分析体系的入口。"

**第 5 幕：可追溯与安全（60 秒）**
展开溯源面板：SQL、口径定义、数据来源、统计周期全可见。
演示越权查询被拦截。讲解："分析结果必须能回溯，AI 不能是黑盒。"

**第 6 幕：主动洞察（30 秒）**
首页洞察卡片："检测到某课程挂科率环比上升 23%"。

---

## 八、风险与对策

| 风险 | 严重度 | 对策 |
| --- | --- | --- |
| **Text2SQL 准确率不够** | 高 | 口径库做厚 + 强制澄清 + 空结果自动重试；演示只走验证过的路径；文档诚实写出准确率数字 |
| **被判定"简单复刻已有产品"** | 高 | 口径治理必须是骨架不是附加功能；演示第 2 幕是核心区分点 |
| 口径库整理耗时超预期 | 中 | 先做 25 个核心指标，够支撑演示即可，后续再扩 |
| 多模态 OCR 准确率低 | 中 | 演示时用清晰的报表图；文档说明识别置信度与人工校正入口 |
| 部署环境问题 | 中 | 第 6 周就部署一次，别等到最后 |
| 数据导入耗时 | 低 | 用 DuckDB 直接查 CSV，开发期免导入 |

---

## 九、踩分对照表

| 评分项 | 占比 | 本项目对应功能 |
| --- | --- | --- |
| 大模型集成质量 | 8% | 意图解析、SQL 生成、结论生成、Prompt 稳定性控制 |
| Agent/工作流设计 | 8% | P0-4 九节点编排流水线 + 结果校验重试 + 主动洞察 |
| RAG 技术应用 | 6% | P0-3 口径库向量检索，别名与口语说法匹配 |
| 多模态能力 | 5% | P1-1 报表截图识别 + 语音提问 |
| AI安全与可控性 | 4% | P0-5 权限与注入防护 + P0-6 全程可追溯 + 拒绝编造数字 |
| MCP/工具集成 | 4% | 数据库与 BI 封装为 MCP Server |
| 选题创新 | 10% | 口径治理 + 主动澄清，市面上无人做 |
| 技术方案创新 | 8% | 语义层架构 + 多源口径冲突建模 |
| 交互模式创新 | 7% | 图表即答案 + 澄清选项卡片 + 分析路径回放 |
| 前端代码质量 | 4% | TypeScript 严格模式、组件化、ESLint/Prettier |
| 组件化与架构 | 3% | 分层架构，Agent 编排与 UI 解耦 |
| 前端性能优化 | 3% | 大数据集 Web Worker 处理、虚拟滚动、图表懒加载、代码分割 |
| 场景真实性 | 5% | 真实高校教学场景 + 真实数据 + 用户访谈记录 |
| 方案有效性 | 5% | 对比演示"有口径澄清 vs 无口径澄清"的差异 |
| UI设计与视觉 | 4% | shadcn/ui + Tailwind，统一设计系统，响应式 |
| 前端交互体验 | 3% | 路由流畅、Framer Motion 过渡、加载状态优雅 |
| AI交互体验 | 3% | **P0-7 流式渲染 + 思考态可视化 + 错误边界** |
| 社会价值 | 5% | 提升教学管理决策质量，方法可迁移至其他院校 |
| 商业潜力 | 5% | 可产品化输出给院校；口径治理是通用痛点 |

---

## 十、第一周具体动作

1. **建仓库**（GitHub/Gitee Public），初始化 Next.js 14 + TS + Tailwind + shadcn/ui
2. **下载并摸清 OULAD**：逐表核验字段与枚举值，写一份 `docs/data-dictionary.md`
3. **写教育部数据抓取脚本**：抓 2015-2024 全国与分省数据，输出 CSV
4. **跑通最小闭环**：硬编码一个指标 → 提问 → 生成 SQL → DuckDB 执行 → 出图
5. **开始整理口径库**：先做 10 个核心指标，验证 YAML 结构够不够用

> 第一周结束时应该能录一段 30 秒的粗糙 demo：输入问题、出来一张图。
> 有这个正反馈，后面两个月才走得下去。
